# autonomous-etl
Migrates legacy ETL pipelines (pandas, SQL, shell) into validated, optimised PySpark — using a team of AutoGen agents whose every conclusion is checked against deterministic tools, orchestrated durably by Temporal, and shipped through GitHub Actions to Kubernetes.
